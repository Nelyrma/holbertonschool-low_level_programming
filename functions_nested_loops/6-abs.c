#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @n : the  integer
 * Return: Always 0
 */

int _abs(int n)
{
	int absolute_value = abs(n);

	_putchar('absolute_value');
	_putchar('\n');
	return (0);
}
